# 时间窗口监测功能使用说明

## 功能介绍

时间窗口监测功能允许您设置特定的时间段进行直播监测和录制。在设定的时间窗口内，程序会使用较短的轮询间隔（如60秒）频繁检测直播状态；在时间窗口外，程序会休眠等待下一个时间窗口的到来，避免不必要的网络请求。

## 适用场景

- 主播有固定的直播时间（如每天19:00-23:30）
- 想要在特定时间段密集监测，避免错过直播
- 在其他时间段减少请求，避免被平台封IP
- 节省系统资源和网络带宽

## 配置说明

在 `config/config.ini` 文件的 `[录制设置]` 部分，添加了以下新配置项：

```ini
# 是否启用时间窗口监测（是/否）
是否启用时间窗口监测(是/否) = 是

# 监测开始时间（24小时制，格式：HH:MM）
监测开始时间 = 19:00

# 监测结束时间（24小时制，格式：HH:MM）
监测结束时间 = 23:30

# 时间窗口内的轮询间隔（秒）
时间窗口内轮询间隔(秒) = 60
```

## 配置示例

### 示例1：每天晚上7点到11点半监测

```ini
是否启用时间窗口监测(是/否) = 是
监测开始时间 = 19:00
监测结束时间 = 23:30
时间窗口内轮询间隔(秒) = 60
```

**效果**：
- 19:00-23:30 期间：每60秒检测一次直播状态
- 其他时间：程序休眠，等待下一个19:00

### 示例2：跨天监测（晚上10点到凌晨2点）

```ini
是否启用时间窗口监测(是/否) = 是
监测开始时间 = 22:00
监测结束时间 = 02:00
时间窗口内轮询间隔(秒) = 60
```

**效果**：
- 22:00-次日02:00 期间：每60秒检测一次
- 其他时间：程序休眠

### 示例3：全天监测，不使用时间窗口

```ini
是否启用时间窗口监测(是/否) = 否
循环时间(秒) = 500
```

**效果**：
- 使用传统方式，每500秒检测一次（全天24小时）

## 工作原理

1. **时间窗口内**：
   - 程序每隔指定秒数（如60秒）检测一次直播状态
   - 如果检测到直播，立即开始录制
   - 录制结束后30秒会再次检测（防止主播卡顿）

2. **时间窗口外**：
   - 程序计算距离下一个监测窗口的时间
   - 进入休眠状态，节省资源
   - 显示倒计时，告知何时开始下一次监测
   - 已经在录制的直播会继续录制完成

3. **特殊情况**：
   - 如果在窗口结束时正在录制，会继续录制直到直播结束
   - 程序会自动处理跨天的时间窗口（如22:00-02:00）

## 使用建议

### 轮询间隔设置

- **60秒**：适合一般情况，不容易错过直播
- **30秒**：适合直播时间不固定或直播时长较短的主播
- **120秒**：适合直播时长较长、不担心错过开头几分钟的情况

### 时间窗口设置

建议将时间窗口设置得比主播实际直播时间略宽一些：

```
主播直播时间：19:30-22:30
建议设置：    19:00-23:00
```

这样可以确保捕捉到提前开播或延后下播的情况。

## 运行效果

启动程序后，您会看到如下提示：

**在时间窗口内启动**：
```
⏰ 时间窗口监测已启用: 19:00-23:30 (当前在窗口内，轮询间隔60秒)
```

**在时间窗口外启动**：
```
⏰ 时间窗口监测已启用: 19:00-23:30 (当前在窗口外，5小时30分钟后开始监测)
```

**运行时状态显示**：
```
共监测3个直播中 | 同一时间访问网络的线程数: 4 | 是否开启代理录制: 否 | 
时间窗口监测: 开启(19:00-23:30) [窗口内] | 录制分段开启: 1800秒 | 
录制视频质量为: 原画 | 录制视频格式为: TS | 目前瞬时错误数为: 0 | 当前时间: 19:45:30
```

**等待下一个窗口时**：
```
主播名 等待监测窗口 05:30:25
```

## 注意事项

1. **时间格式**：必须使用24小时制，格式为 `HH:MM`（如 19:00、02:30）
2. **跨天时间**：支持跨天设置（如 22:00-02:00），程序会自动处理
3. **已有录制**：如果在窗口外还有正在进行的录制，会继续完成
4. **配置修改**：修改配置后需要重启程序才能生效
5. **与原有功能兼容**：可以随时关闭此功能，恢复原有的固定轮询方式

## 对比说明

### 使用时间窗口前
- 全天24小时，每500秒检测一次
- 主播7点开播，可能在7点-7点8分之间才检测到
- 容易错过短时直播

### 使用时间窗口后
- 19:00-23:30，每60秒检测一次
- 主播7点开播，最多1分钟内就能检测到
- 其他时间休眠，节省资源和避免频繁请求

## 问题排查

如果时间窗口功能没有按预期工作：

1. 检查配置文件格式是否正确
2. 确认时间格式为 `HH:MM`（两位数字，用冒号分隔）
3. 查看日志文件中是否有错误信息
4. 确保已重启程序使配置生效

---

**祝您使用愉快！如有问题，请查看项目的 GitHub Issues。**

