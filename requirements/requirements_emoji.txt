# 表情包生成系统依赖

# 核心依赖
opencv-python>=4.8.0        # 视频处理、帧提取
Pillow>=10.0.0              # 图像处理、GIF生成
numpy>=1.24.0               # 数值计算

# GIF生成
imageio>=2.31.0             # 图像IO、GIF动画
imageio-ffmpeg>=0.4.9       # FFmpeg后端

# 姿态估计（可选，用于精确手势识别）
mediapipe>=0.10.0           # 手部和姿态检测

# 已有依赖（来自主项目）
# 如果这些包已安装，无需重复安装
# opencv-python>=4.8.0
# numpy>=1.24.0



